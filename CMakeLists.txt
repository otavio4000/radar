cmake_minimum_required(VERSION 3.13.1)

find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(embedded_systems_project)

target_sources(app PRIVATE
    src/main.c
    src/ipc.c
    src/sensors.c
    src/controller.c
    src/camera.c
    src/display_dummy.c
    src/input_thread.c
    tests/integration/vehicle_simulator.c
)